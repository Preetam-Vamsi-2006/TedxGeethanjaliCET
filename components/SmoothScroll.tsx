"use client";

import { ReactNode } from "react";

export default function SmoothScroll({ children }: { children: ReactNode }) {
  // Smooth scrolling is handled by CSS and browser defaults
  // No external library needed for basic smooth scroll
  return <>{children}</>;
}
